{
    "name": "billing-software",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix desktop\"",
        "dev:desktop": "concurrently \"npm run dev --prefix frontend\" \"wait-on http://localhost:5173 && npm run dev --prefix desktop\"",
        "build": "npm run build --prefix frontend",
        "start": "npm run start --prefix desktop"
    },
    "devDependencies": {
        "concurrently": "^9.2.1",
        "wait-on": "^7.2.0"
    }
}
